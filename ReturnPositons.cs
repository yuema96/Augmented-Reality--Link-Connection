using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReturnPositons : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {   
        //GameObject.Find("PointInner4").transform.position = GameObject.Find("Cube").transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
